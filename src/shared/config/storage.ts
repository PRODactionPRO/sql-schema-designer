// ── Shared storage constants ──
// Single source of truth for localStorage key prefixes

export const STORAGE_PROJECT_PREFIX = 'drawsql_project_';
export const STORAGE_INDEX_KEY = 'drawsql_project_index';
